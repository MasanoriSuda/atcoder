#include <bits/stdc++.h>
using namespace std;

int main(void)
{
    int N;
    vector<int> A;

    for (int i = 0; i < N; i++)
    {
        int tmp;
        cin >> tmp;
        A.emplace_back(tmp);
    }

    sort(A.begin(), A.end());

    int kugiuchi = *A.begin();

    for ()
}
